# Epidura
Epidura is a Rust-based tool for Staged Remote Process Injection via IPC orchestration. By separating the dormant stager (pipe server) from the external trigger, it bypasses EDR flagging and avoids noisy shellcode.
